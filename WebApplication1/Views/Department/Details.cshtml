@model Department

<table class="table table-dark">
        <tr>
            <th scope="col">@Model.Id</th>
            <th scope="col">@Model.Name</th>
            <th scope="col">@Model.ManagerName</th>  
        </tr>
</table>

@Html.TextBoxFor(dept => dept.Name,new{style="color:red",@class="form-control"});